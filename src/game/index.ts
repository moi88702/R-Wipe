/**
 * Game logic layer — entry point.
 *
 * This module will house the core game loop, state machine, and scene
 * controller. Implementations will be added as each feature is built out.
 */

export {}; // ensure this is treated as a module

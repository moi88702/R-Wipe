/** State for the NPC-talk / mission-detail screens. Logic lives in GameManager. */
export class NpcTalkMissionHandler {
  activeTalkNpcId: string | null = null;
  activeMissionDetailId: string | null = null;
}

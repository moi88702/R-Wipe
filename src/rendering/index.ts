/**
 * Pixi.js rendering layer — entry point.
 *
 * This module will provide the Pixi container hierarchy (stage, game, hud),
 * sprite management, particle effects, and text rendering utilities.
 * Implementations will be added as each visual feature is built out.
 */

// HUD Visual Feedback System
export { HUDRenderer, type HUDRenderData, type WaypointMarker } from "./HUDRenderer";
